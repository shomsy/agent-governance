# Execution Governance

This folder contains rules that shape how work is executed:

- `policy/`: task start, validation, and finish contract
- `hooks/`: lifecycle interception points
- `approvals/`: trust tiers and approval rules
- `sandbox/`: hard boundaries for untrusted code execution
