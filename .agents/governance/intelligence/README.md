# Intelligence Governance

This folder groups reusable knowledge and learning rules:

- `memory/`: memory lifecycle and memory standard
- `context/`: context-window budgeting and compaction rules
- `learning/`: observations, instincts, and evolution policy
