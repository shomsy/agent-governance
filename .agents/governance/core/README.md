# Core Governance

Use this folder for the reusable control plane:

- `quality/`: universal completion and quality gates
- `resolution/`: deterministic rule-pack resolution
- `flags/`: feature toggles for conditional governance activation
