# Dashboard UI Baseline

Status: Baseline implemented.

Scope:

- SPA shell (`index.html`, `app.js`, `styles.css`)
- Event stream client contract (`event-stream.js`)
- Telemetry contract (`telemetry.js`)

Boundary:

- Dashboard is read-oriented and evidence-focused.
- It must not mutate infrastructure law or bypass engine gates.
